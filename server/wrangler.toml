name = "whiteboard-server"
main = "src/index.js"
compatibility_date = "2024-04-03"

[[durable_objects.bindings]]
name = "WHITEBOARD_ROOM"
class_name = "WhiteboardRoom"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["WhiteboardRoom"]

# Before enabling cloud boards, create a D1 database and add this binding with
# the real database ID (do not commit credentials or placeholder IDs):
# [[d1_databases]]
# binding = "DB"
# database_name = "whiteboard-metadata"
# database_id = "<database-id-from-cloudflare>"
